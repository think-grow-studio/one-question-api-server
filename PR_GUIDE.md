# Pull Request 작성 가이드 (`feature` → `develop`)

이 문서는 기능 브랜치(`feature/*`)에서 작업을 마친 뒤 `develop` 브랜치로
Pull Request를 올릴 때 따라야 하는 절차와 작성 규칙을 정의합니다.

> 목표: **변경의 큰 흐름은 한눈에**, **세부 변경은 명확하고 자세하게** 전달하는 PR을 만든다.

---

## 1. PR 올리기 전 준비

### 1-1. 브랜치 최신화
`develop`의 최신 변경을 먼저 반영해 충돌을 줄입니다.

```bash
git checkout develop
git pull origin develop

git checkout feature/your-branch
git merge develop        # 또는 git rebase develop
```

### 1-2. 변경 사항 직접 확인
PR 본문을 쓰기 전에 **반드시** 아래 두 가지를 확인합니다.

```bash
# 1) 큰 흐름 파악 — 어떤 작업들을 했는지 (커밋 단위)
git log develop..HEAD --oneline

# 2) 세부 차이 파악 — 실제로 무엇이 어떻게 바뀌었는지
git diff develop...HEAD              # 전체 diff
git diff develop...HEAD --stat       # 파일별 변경 규모 요약
```

- **`git log`** → PR의 "개요 / 주요 변경 사항"의 뼈대가 됩니다.
- **`git diff`** → 각 변경의 의도·동작을 구체적으로 서술하는 근거가 됩니다.

### 1-3. 셀프 체크
- [ ] 빌드 / 테스트 통과 (`./gradlew test`)
- [ ] 디버그용 코드, 주석 처리한 코드, 불필요한 로그 제거
- [ ] 커밋 메시지가 컨벤션(`feat:`, `fix:`, `test:` 등)을 따르는지
- [ ] 관련 없는 변경(포맷팅, 무관한 파일)이 섞여 있지 않은지

---

## 2. PR 기본 규칙

| 항목 | 규칙 |
| --- | --- |
| **Base 브랜치** | `develop` (절대 `main`이 아님) |
| **Compare 브랜치** | `feature/*` 작업 브랜치 |
| **제목** | `<타입>: <한 줄 요약>` — 대표 커밋 메시지와 일치시킴 |
| **언어** | 본문은 한국어로 작성 |
| **크기** | 가능하면 리뷰 가능한 단위로 작게 유지 (변경 규모가 크면 분리 검토) |

### 제목 컨벤션 예시
```
feat: 질문 응답에 전체 좋아요 수(likeCount) 포함
fix: 일일 질문 리로드 시 후보 중복 노출 버그 수정
refactor: QuestionApplication 좋아요 조회 로직 분리
```

---

## 3. PR 본문 템플릿

아래 템플릿을 복사해 사용합니다. (`.github/pull_request_template.md`로 두면 자동 적용)

```markdown
## 📌 개요
<!-- 이 PR이 무엇을, 왜 하는지 2~3줄로 요약. git log의 큰 흐름을 한 문단으로 -->

## 🎯 변경 동기 / 배경
<!-- 어떤 문제/요구사항 때문에 이 작업을 했는지. 관련 이슈가 있으면 링크 -->
- 

## 🔧 주요 변경 사항
<!-- git diff 기반으로 '계층/모듈별'로 묶어 구체적으로 작성.
     - 무엇을 추가/수정/삭제했는지
     - 왜 그렇게 했는지 (설계 의도)
     - 핵심 로직은 짧은 코드 스니펫 인용 -->

### 1. <계층 또는 모듈 이름>
- 

### 2. <계층 또는 모듈 이름>
- 

## 🧪 테스트
<!-- 추가/수정한 테스트와 검증 포인트. 표로 정리하면 가독성 좋음 -->
| 테스트 | 검증 내용 |
| --- | --- |
|  |  |

## 📷 스크린샷 / 응답 예시
<!-- API 응답 변화, UI 변화 등이 있으면 첨부 (선택) -->

## ⚠️ 주의 사항 / 영향 범위
<!-- Breaking Change, 마이그레이션, 환경변수, 배포 순서 등 (없으면 '없음') -->
- 

## ✅ 체크리스트
- [ ] 빌드 / 테스트 통과
- [ ] 자체 리뷰 완료
- [ ] 문서 / API 스펙(Swagger 등) 반영
- [ ] Breaking Change 여부 확인
```

---

## 4. 본문 작성 원칙

### 4-1. "큰 흐름 → 세부"의 순서로 쓴다
- **개요**는 커밋 로그를 한 문단으로 압축한 큰 그림.
- **주요 변경 사항**은 diff를 근거로 계층/모듈별로 나누어 자세히.

### 4-2. "무엇을" 보다 "왜"를 남긴다
diff만 보면 *무엇이* 바뀌었는지는 알 수 있습니다.
PR 본문에는 코드에 드러나지 않는 **설계 의도와 이유**를 적습니다.

```
❌ likeCountMap 파라미터를 추가함
✅ 다건 조회 시 질문마다 카운트 쿼리가 나가는 N+1을 막기 위해,
   질문 ID를 모아 한 번의 배치 집계로 조회하도록 likeCountMap을 주입
```

### 4-3. 핵심 로직은 코드 스니펫으로 인용
리뷰어가 파일을 열지 않아도 이해할 수 있도록, 변경의 핵심이 되는
쿼리·메서드 시그니처·분기 로직은 짧게 인용합니다.

### 4-4. 테스트로 "동작 보증"을 보여준다
어떤 시나리오를 어떻게 검증했는지 명시합니다. (정상/경계/예외 케이스)

---

## 5. PR 생성 명령어 (선택)

GitHub CLI 사용 시:

```bash
gh pr create \
  --base develop \
  --head feature/your-branch \
  --title "feat: <한 줄 요약>" \
  --body-file PR_BODY.md
```

---

## 6. 리뷰 & 머지

- 최소 **1명 이상**의 리뷰 승인 후 머지
- 리뷰 코멘트는 반영 후 resolve, 추가 논의가 필요하면 스레드 유지
- 머지 전략: **Squash and merge** 권장 (커밋 히스토리를 깔끔하게 유지)
- 머지 후 기능 브랜치는 삭제
